node_modules/
dist/
*.log
*.tsbuildinfo

# Personal config — each user has their own; example committed instead
vault-sidekick.config.yaml

# Obsidian plugin build artifacts (built locally; install.sh rebuilds on demand)
obsidian-plugin/main.js
obsidian-plugin/main.js.map

# OS / editors
.DS_Store
.vscode/
.idea/
